function entities(state = {}, action) {
  switch (action.type) {
    case 'ADD_ENTITY':
      return state
    case 'REMOVE_ENTITY':
      return state
    default:
      return state
  }
}

export default entities;
